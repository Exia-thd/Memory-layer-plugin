{
  "description": "Loads active constraints at session start, offers memory context when the agent reaches for raw search, and checks a commit against what memory already records.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "timeout": 10,
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/memory-hook.mjs\" session-start",
            "statusMessage": "Loading project memory..."
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "timeout": 10,
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/memory-hook.mjs\" pre-commit",
            "statusMessage": "Checking memory against the staged change..."
          }
        ]
      },
      {
        "matcher": "Read|Grep|Glob",
        "hooks": [
          {
            "type": "command",
            "timeout": 10,
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/memory-hook.mjs\" pre-tool",
            "statusMessage": "Checking project memory..."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "timeout": 10,
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/memory-hook.mjs\" post-tool",
            "statusMessage": "Recording to project memory..."
          }
        ]
      }
    ]
  }
}
